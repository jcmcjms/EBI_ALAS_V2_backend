{
  "ConnectionStrings": {
    "DefaultConnection": "Server=<DB_HOST>;Database=ALASv2_DB;User Id=<DB_USER>;Password=<DB_PASSWORD>;TrustServerCertificate=True;Encrypt=True;Max Pool Size=300;Min Pool Size=20;Connection Lifetime=300;Connection Timeout=30",
    "WebLoanConnection": "Server=<DB_HOST>;Database=webloan;User Id=<DB_USER>;Password=<DB_PASSWORD>;TrustServerCertificate=True;Encrypt=True;Max Pool Size=100;Min Pool Size=10;Connection Lifetime=300;Connection Timeout=30",
    "_Redis_comment": "REQUIRED for multi-replica production deployments. The JTI revocation blacklist (every authenticated request) and the idempotency middleware both read this. Without Redis, IDistributedCache falls back to per-pod in-memory — the blacklist becomes pod-local and a revoked token on pod A is still accepted on pod B until it expires (15 min). Recommended server: maxmemory 256mb, maxmemory-policy allkeys-lru.",
    "Redis": ""
  },
  "Jwt": {
    "SecretKey": "<JWT_SECRET_KEY_MIN_32_CHARS>",
    "Issuer": "EBI.ALAS.V2",
    "Audience": "EBI.ALAS.V2.Frontend",
    "ExpiryMinutes": 15,
    "RefreshTokenExpiryDays": 7,
    "AbsoluteSessionExpiryDays": 14
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "https://localhost:3000",
      "http://localhost:5173",
      "https://localhost:5173"
    ]
  },
  "RateLimiting": {
    "Login": {
      "PermitLimit": 5,
      "WindowSeconds": 60
    },
    "Data": {
      "PermitLimit": 120,
      "WindowSeconds": 60
    }
  },
  "LoanProductSync": {
    "_comment": "How often the LoanProductSyncHostedService runs to pull webloan's loan_product catalog into the ALAS mirror. Default 360 minutes (6h) — policy changes are infrequent, this is a mirror, not a transactional job.",
    "IntervalMinutes": 360
  },
  "TokenCleanup": {
    "_comment": "How often CleanupExpiredTokensHostedService runs to bulk-delete expired RefreshTokens (sliding + absolute expiry) and RevokedTokens (JTI blacklist past access-token expiry). Default 60 minutes (1h). The hot table is RevokedTokens — every logout adds a row that becomes eligible for delete 15 minutes later; 1h means worst case ~4× per-hour churn sits in the table, well within indexed-seek range.",
    "IntervalMinutes": 60
  },
  "LoanAttachments": {
    "StoragePath": "App_Data/loan-attachments",
    "MaxFileSizeBytes": 10485760,
    "MaxFilesPerLoan": 25
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
